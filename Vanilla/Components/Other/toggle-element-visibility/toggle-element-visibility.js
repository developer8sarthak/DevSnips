/* Snippet Name: Toggle Element Visibility | Use Case: Toggle the visibility of an HTML element | Description: A one-liner to toggle the visibility of an element by changing its display style. */
const toggleVisibility = (element) => (element.style.display = element.style.display === 'none' ? 'block' : 'none');
