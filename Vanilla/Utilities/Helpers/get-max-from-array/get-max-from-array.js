/* Snippet Name: Get Max from Array | Use Case: Find the maximum value in an array of numbers | Description: A one-liner to get the maximum value from an array of numbers. */
const getMax = (arr) => Math.max(...arr);
