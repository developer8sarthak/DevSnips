/* Snippet Name: Is Browser Tab in View | Use Case: Check if the current browser tab is in focus | Description: A one-liner to determine if the current browser tab is visible or hidden. */
const isTabInView = () => !document.hidden;
