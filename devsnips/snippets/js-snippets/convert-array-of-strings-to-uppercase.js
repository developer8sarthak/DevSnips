/* Snippet Name: Convert Array of Strings to Uppercase | Use Case: Convert all string elements in an array to uppercase | Description: A one-liner to convert all string elements in an array to their uppercase equivalent. */
const toUpperCaseArray = (arr) => arr.map((s) => s.toUpperCase());
