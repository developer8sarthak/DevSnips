/* Snippet Name: Redirect to URL | Use Case: Redirect the user to a different URL | Description: A one-liner to redirect the browser to a new URL. */
const redirectTo = (url) => (window.location.href = url);
