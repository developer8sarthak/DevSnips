/* Snippet Name: Capitalize String | Use Case: Capitalize the first letter of a string | Description: A simple function to capitalize the first letter of a given string. */
const capitalize = (s) => s.charAt(0).toUpperCase() + s.slice(1);
