/* Snippet Name: Is Object Empty | Use Case: Check if an object has no properties | Description: A one-liner to check if an object is empty. */
const isObjectEmpty = (obj) => Object.keys(obj).length === 0;
