/* Snippet Name: Get Min from Array | Use Case: Find the minimum value in an array of numbers | Description: A one-liner to get the minimum value from an array of numbers. */
const getMin = (arr) => Math.min(...arr);
