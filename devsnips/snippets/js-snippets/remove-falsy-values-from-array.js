/* Snippet Name: Remove Falsy Values from Array | Use Case: Remove falsy values from an array | Description: A one-liner to remove values like false, null, 0, "", undefined, and NaN from an array. */
const removeFalsy = (arr) => arr.filter(Boolean);
