/* Snippet Name: Is Number | Use Case: Check if a value is a number | Description: A one-liner to check if a given value is a number. */
const isNumber = (value) => typeof value === 'number' && isFinite(value);
