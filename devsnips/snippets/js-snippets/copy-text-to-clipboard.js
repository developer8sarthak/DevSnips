/* Snippet Name: Copy Text to Clipboard | Use Case: Copy text to the user's clipboard | Description: A modern and secure way to copy text to the clipboard. */
const copyToClipboard = (text) => navigator.clipboard.writeText(text);
