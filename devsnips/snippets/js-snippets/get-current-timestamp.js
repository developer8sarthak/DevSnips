/* Snippet Name: Get Current Timestamp | Use Case: Get the current timestamp in milliseconds | Description: Returns the number of milliseconds since the ECMAScript epoch. */
const getTimestamp = () => Date.now();
