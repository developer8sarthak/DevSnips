/* Snippet Name: Get Random Element from Array | Use Case: Get a random element from an array | Description: A one-liner to get a random element from an array. */
const randomElement = (arr) => arr[Math.floor(Math.random() * arr.length)];
