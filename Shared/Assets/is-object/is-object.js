/* Snippet Name: Is Object | Use Case: Check if a value is an object | Description: A one-liner to check if a given value is an object. */
const isObject = (value) => value !== null && typeof value === 'object';
